[workspace]
release_always = false

# Semantic crates use release-plz's workspace default `<package>-v<version>` tag.
# `cordis-rs` preserves its historical `v<version>` tag lineage.
[[package]]
name = "cordis-rs"
git_tag_name = "v{{ version }}"
git_release_name = "v{{ version }}"

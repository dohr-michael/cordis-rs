[package]
name = "worker_daemon"
description = "cordis example — the worker-daemon seat (crash-loop / heal / drain)"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
cordis-core = { workspace = true }
cordis-timer = { workspace = true }
examples-common = { workspace = true }
futures = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["time", "test-util"] }

[lints]
workspace = true
